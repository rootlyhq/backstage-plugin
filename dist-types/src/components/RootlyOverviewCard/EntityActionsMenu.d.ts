/// <reference types="react" />
import { Entity } from '../../types';
export declare const RootlyEntityServiceActionsMenu: ({ entity, handleUpdate, handleImport, handleDelete, }: {
    entity: Entity;
    handleUpdate: Function;
    handleImport: Function;
    handleDelete: Function;
}) => JSX.Element;
