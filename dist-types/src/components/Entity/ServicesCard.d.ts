/// <reference types="react" />
import { InfoCardVariants } from '@backstage/core-components';
declare type ServicesCardProps = {
  title?: string;
  variant?: InfoCardVariants;
};
export declare const ServicesCard: ({
  title,
  variant,
}: ServicesCardProps) => JSX.Element;
export {};
