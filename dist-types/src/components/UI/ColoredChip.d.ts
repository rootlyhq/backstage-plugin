import React from 'react';
export declare const ColoredChip: ({ label, tooltip, color, icon, onClick, }: {
    label?: string;
    tooltip?: string;
    color?: string;
    icon?: React.ReactElement;
    onClick?: () => void;
}) => React.JSX.Element;
